Place your resume PDF here as:

  Prince-Kanani-Resume.pdf

The site checks for this file at runtime — the download/view buttons appear
automatically once it exists. The filename is configurable in
src/config/siteConfig.ts (resumePath).
